# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: Deploy to Firebase Hosting on PR
'on': pull_request
jobs:
  build_and_preview:
    if: '${{ github.event.pull_request.head.repo.full_name == github.repository }}'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - run: npm ci && npm run build
      - uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: '${{ secrets.GITHUB_TOKEN }}'
          firebaseServiceAccount: '${{ secrets.FIREBASE_SERVICE_ACCOUNT_AKOS_PORTFOLIO }}'
          projectId: akos-portfolio
# name: Firebase Hosting PR Deploy

# on:
#   pull_request:
#     branches:
#       - main

# jobs:
#   build_and_deploy:
#     runs-on: ubuntu-latest
#     steps:
#       - uses: actions/checkout@v2
#       - name: Set up Node.js
#         uses: actions/setup-node@v2
#         with:
#           node-version: '14.x'
#       - name: Install Firebase CLI
#         run: npm install -g firebase-tools
#       - name: Authenticate Firebase
#         env:
#           FIREBASE_TOKEN: 1//01icXdM54V9KBCgYIARAAGAESNwF-L9Irm_Xhxyv0lb_xa9MrLOFkMawsvB1kdus6dGmG_JJyZhZD7XOaLFZpomy8cTKFruY0508
#         run: firebase login:ci --no-localhost
#       - name: Deploy to Firebase Hosting
#         env:
#           FIREBASE_PROJECT_ID: akos-portfolio
#         run: firebase deploy --project akos-portfolio --only hosting:PR-$GITHUB_HEAD_REF
